// @autocashier/shared - Barrel export
// Shared code used across all AutoCashier apps (admin, kasir, member) and backend

// Supabase client singleton
export { supabase } from './supabase';

// Shared TypeScript types
export * from './types';

// Shared utility functions
// export * from './utils';

// Shared styles are imported directly via:
// @import "@autocashier/shared/styles/base.css";
